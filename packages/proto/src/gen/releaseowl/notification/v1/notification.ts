// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: releaseowl/notification/v1/notification.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "releaseowl.notification.v1";

/**
 * NotifyRequest carries exactly one email kind via a oneof.  Clients MUST
 * populate one of the two variants; an empty oneof yields INVALID_ARGUMENT.
 */
export interface NotifyRequest {
  kind?: { $case: "confirmation"; confirmation: ConfirmationEmail } | {
    $case: "notification";
    notification: NotificationEmail;
  } | undefined;
}

/**
 * ConfirmationEmail is sent when a new subscription is created.  The
 * recipient must follow the link to activate the subscription.
 */
export interface ConfirmationEmail {
  email: string;
  repo: string;
  /** confirm_token is the single-use activation token embedded in the link. */
  confirmToken: string;
}

/**
 * NotificationEmail is sent when a new release is published on a watched
 * repository.
 */
export interface NotificationEmail {
  email: string;
  repo: string;
  tagName: string;
  /** unsubscribe_token allows one-click unsubscription from the email footer. */
  unsubscribeToken: string;
}

/**
 * NotifyResponse is intentionally empty; success is signalled by the OK
 * status code alone.
 */
export interface NotifyResponse {
}

function createBaseNotifyRequest(): NotifyRequest {
  return { kind: undefined };
}

export const NotifyRequest: MessageFns<NotifyRequest> = {
  encode(message: NotifyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    switch (message.kind?.$case) {
      case "confirmation":
        ConfirmationEmail.encode(message.kind.confirmation, writer.uint32(10).fork()).join();
        break;
      case "notification":
        NotificationEmail.encode(message.kind.notification, writer.uint32(18).fork()).join();
        break;
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotifyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotifyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.kind = { $case: "confirmation", confirmation: ConfirmationEmail.decode(reader, reader.uint32()) };
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.kind = { $case: "notification", notification: NotificationEmail.decode(reader, reader.uint32()) };
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotifyRequest {
    return {
      kind: isSet(object.confirmation)
        ? { $case: "confirmation", confirmation: ConfirmationEmail.fromJSON(object.confirmation) }
        : isSet(object.notification)
        ? { $case: "notification", notification: NotificationEmail.fromJSON(object.notification) }
        : undefined,
    };
  },

  toJSON(message: NotifyRequest): unknown {
    const obj: any = {};
    if (message.kind?.$case === "confirmation") {
      obj.confirmation = ConfirmationEmail.toJSON(message.kind.confirmation);
    } else if (message.kind?.$case === "notification") {
      obj.notification = NotificationEmail.toJSON(message.kind.notification);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotifyRequest>, I>>(base?: I): NotifyRequest {
    return NotifyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotifyRequest>, I>>(object: I): NotifyRequest {
    const message = createBaseNotifyRequest();
    switch (object.kind?.$case) {
      case "confirmation": {
        if (object.kind?.confirmation !== undefined && object.kind?.confirmation !== null) {
          message.kind = {
            $case: "confirmation",
            confirmation: ConfirmationEmail.fromPartial(object.kind.confirmation),
          };
        }
        break;
      }
      case "notification": {
        if (object.kind?.notification !== undefined && object.kind?.notification !== null) {
          message.kind = {
            $case: "notification",
            notification: NotificationEmail.fromPartial(object.kind.notification),
          };
        }
        break;
      }
    }
    return message;
  },
};

function createBaseConfirmationEmail(): ConfirmationEmail {
  return { email: "", repo: "", confirmToken: "" };
}

export const ConfirmationEmail: MessageFns<ConfirmationEmail> = {
  encode(message: ConfirmationEmail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.repo !== "") {
      writer.uint32(18).string(message.repo);
    }
    if (message.confirmToken !== "") {
      writer.uint32(26).string(message.confirmToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfirmationEmail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfirmationEmail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.confirmToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfirmationEmail {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      repo: isSet(object.repo) ? globalThis.String(object.repo) : "",
      confirmToken: isSet(object.confirmToken)
        ? globalThis.String(object.confirmToken)
        : isSet(object.confirm_token)
        ? globalThis.String(object.confirm_token)
        : "",
    };
  },

  toJSON(message: ConfirmationEmail): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    if (message.confirmToken !== "") {
      obj.confirmToken = message.confirmToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConfirmationEmail>, I>>(base?: I): ConfirmationEmail {
    return ConfirmationEmail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConfirmationEmail>, I>>(object: I): ConfirmationEmail {
    const message = createBaseConfirmationEmail();
    message.email = object.email ?? "";
    message.repo = object.repo ?? "";
    message.confirmToken = object.confirmToken ?? "";
    return message;
  },
};

function createBaseNotificationEmail(): NotificationEmail {
  return { email: "", repo: "", tagName: "", unsubscribeToken: "" };
}

export const NotificationEmail: MessageFns<NotificationEmail> = {
  encode(message: NotificationEmail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.email !== "") {
      writer.uint32(10).string(message.email);
    }
    if (message.repo !== "") {
      writer.uint32(18).string(message.repo);
    }
    if (message.tagName !== "") {
      writer.uint32(26).string(message.tagName);
    }
    if (message.unsubscribeToken !== "") {
      writer.uint32(34).string(message.unsubscribeToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotificationEmail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotificationEmail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.email = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.repo = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.tagName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.unsubscribeToken = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NotificationEmail {
    return {
      email: isSet(object.email) ? globalThis.String(object.email) : "",
      repo: isSet(object.repo) ? globalThis.String(object.repo) : "",
      tagName: isSet(object.tagName)
        ? globalThis.String(object.tagName)
        : isSet(object.tag_name)
        ? globalThis.String(object.tag_name)
        : "",
      unsubscribeToken: isSet(object.unsubscribeToken)
        ? globalThis.String(object.unsubscribeToken)
        : isSet(object.unsubscribe_token)
        ? globalThis.String(object.unsubscribe_token)
        : "",
    };
  },

  toJSON(message: NotificationEmail): unknown {
    const obj: any = {};
    if (message.email !== "") {
      obj.email = message.email;
    }
    if (message.repo !== "") {
      obj.repo = message.repo;
    }
    if (message.tagName !== "") {
      obj.tagName = message.tagName;
    }
    if (message.unsubscribeToken !== "") {
      obj.unsubscribeToken = message.unsubscribeToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NotificationEmail>, I>>(base?: I): NotificationEmail {
    return NotificationEmail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotificationEmail>, I>>(object: I): NotificationEmail {
    const message = createBaseNotificationEmail();
    message.email = object.email ?? "";
    message.repo = object.repo ?? "";
    message.tagName = object.tagName ?? "";
    message.unsubscribeToken = object.unsubscribeToken ?? "";
    return message;
  },
};

function createBaseNotifyResponse(): NotifyResponse {
  return {};
}

export const NotifyResponse: MessageFns<NotifyResponse> = {
  encode(_: NotifyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NotifyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNotifyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): NotifyResponse {
    return {};
  },

  toJSON(_: NotifyResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<NotifyResponse>, I>>(base?: I): NotifyResponse {
    return NotifyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NotifyResponse>, I>>(_: I): NotifyResponse {
    const message = createBaseNotifyResponse();
    return message;
  },
};

/**
 * NotificationService provides a synchronous email-send path that runs
 * parallel to the default async RabbitMQ broker path.  Both paths share
 * the same logical payload (confirmation / notification email) but this
 * one bypasses the Saga — it is a direct request-response call.
 */
export type NotificationServiceService = typeof NotificationServiceService;
export const NotificationServiceService = {
  /**
   * Notify sends a single email synchronously and returns only after the
   * send has been attempted.  On success the response is empty.  On
   * failure the status code reflects the failure class:
   *   INVALID_ARGUMENT — malformed request (missing kind, missing email…)
   *   UNAVAILABLE      — transient SMTP / network failure (safe to retry)
   *   INTERNAL         — unexpected server-side error
   */
  notify: {
    path: "/releaseowl.notification.v1.NotificationService/Notify" as const,
    requestStream: false as const,
    responseStream: false as const,
    requestSerialize: (value: NotifyRequest): Buffer => Buffer.from(NotifyRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): NotifyRequest => NotifyRequest.decode(value),
    responseSerialize: (value: NotifyResponse): Buffer => Buffer.from(NotifyResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): NotifyResponse => NotifyResponse.decode(value),
  },
} as const;

export interface NotificationServiceServer extends UntypedServiceImplementation {
  /**
   * Notify sends a single email synchronously and returns only after the
   * send has been attempted.  On success the response is empty.  On
   * failure the status code reflects the failure class:
   *   INVALID_ARGUMENT — malformed request (missing kind, missing email…)
   *   UNAVAILABLE      — transient SMTP / network failure (safe to retry)
   *   INTERNAL         — unexpected server-side error
   */
  notify: handleUnaryCall<NotifyRequest, NotifyResponse>;
}

export interface NotificationServiceClient extends Client {
  /**
   * Notify sends a single email synchronously and returns only after the
   * send has been attempted.  On success the response is empty.  On
   * failure the status code reflects the failure class:
   *   INVALID_ARGUMENT — malformed request (missing kind, missing email…)
   *   UNAVAILABLE      — transient SMTP / network failure (safe to retry)
   *   INTERNAL         — unexpected server-side error
   */
  notify(
    request: NotifyRequest,
    callback: (error: ServiceError | null, response: NotifyResponse) => void,
  ): ClientUnaryCall;
  notify(
    request: NotifyRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: NotifyResponse) => void,
  ): ClientUnaryCall;
  notify(
    request: NotifyRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: NotifyResponse) => void,
  ): ClientUnaryCall;
}

export const NotificationServiceClient = makeGenericClientConstructor(
  NotificationServiceService,
  "releaseowl.notification.v1.NotificationService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): NotificationServiceClient;
  service: typeof NotificationServiceService;
  serviceName: string;
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
